version: '3.8'

services:
  frontend-service:
    build: ./frontend
    ports:
      - 3000:3000
    volumes:
      - ./frontend:/frontend
      - /frontend/node_modules
    stdin_open: true
    tty: true

    environment:
      - CHOKIDAR_USEPOLLING=true
